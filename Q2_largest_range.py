def find_largest_range(array):
    """
    Find the largest range in an array
        - Range is defined as a set of consecutive integers (each value differs by only 1 from its neighbours)
        - Example: [3,6,1,2,15,-4,4] has largest range of 4, composed of [1,2,3,4]. This sould be formatted as [1,4] as the return value

    Parameters:
        array: list
            Array of integers, may not be sorted

    Returns:
        list: A list of two elements indicating the start value and the end value (inclusive) of the max range in array
    """

    pass
